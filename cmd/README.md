# Command Binaries

Entrypoints for executable components:

- `resolver`
- `builder`
- `runner`
- `verifier`

Each subdirectory should contain one deployable binary entrypoint plus minimal integration wiring.
