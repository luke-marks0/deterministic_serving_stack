# Test Layout

- `unit/`: isolated logic and schema tests.
- `integration/`: component interactions.
- `e2e/`: workflow-level execution paths.
- `determinism/`: reproducibility-specific checks.
- `chaos/`: fault and drift scenarios.
- `fixtures/positive/`: fixtures that MUST pass schema validation.
- `fixtures/negative/`: fixtures that MUST fail schema validation.
